Drop these TWO video files into this folder:

  fight.mp4       ← plays when you do any fight gesture (fist, web, palm...)
  look_right.mp4  ← plays when you turn your head to the right

Both loop automatically until the gesture stops.
Supported formats: .mp4  .avi  .mov  .webm  .mkv
